# Copy this file to .env and fill in the values
# Option 1: Provide the path to your service account JSON file
GOOGLE_SHEETS_CREDENTIALS="path/to/your/service-account.json"

# Option 2: Provide the entire JSON content as a string (useful for cloud environments)
# GOOGLE_SHEETS_CREDENTIALS='{"type": "service_account", "project_id": "...", ...}'
